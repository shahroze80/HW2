%h1 All Movies

= form_tag movies_path, :method => :get do
  = hidden_field_tag "title_sort", true if @title_header
  = hidden_field_tag ":release_date_sort", true if @date_header
  Include: 
  - @all_ratings.each do |rating|
    = rating
    = check_box_tag "ratings[#{rating}]",1, @selected_ratings.include?(rating), :id => "ratings_#{rating}"
  = submit_tag 'Refresh'

%table#movies
  %thead
    %tr
      %th{:class => @title_header}= link_to "Movie Title", movies_path(:sort_by => 'title', :ratings => @selected_ratings)
      %th Rating
      %th{:class => @date_header}=link_to "Release Date", movies_path(:sort_by => "release_date", :ratings => @selected_ratings)
      %th More Info
  %tbody
    - @movies.each do |movie|
      %tr        
        %td= movie.title 
        %td= movie.rating
        %td= movie.release_date
        %td= link_to "More about #{movie.title}", movie_path(movie)

= link_to 'Add new movie', new_movie_path
= link_to 'Update a movie', movies_update_path
= link_to 'Delete a movie', movies_delete_path

%section#ib-container.ib-container
  - @movies.each do |movie|
    %article
      %header
        %h3
          %a.nice-link
            = link_to "#{movie.title}", movie_path(movie)
        %span= movie.rating
      %p= movie.release_date


%section
  %h1
    %a.nice-link= link_to 'Add new movie', new_movie_path
  %p
    Lorem ipsum dolor sit amet, consectetur
    %a.nice-link{:href => "#"} adipiscing
    elit fusce vel sapien elit in malesuada semper mi,
    id sollicitudin urna fermentum ut fusce varius nisl ac ipsum gravida vel pretium tellus.
    Lorem ipsum dolor sit amet,
    %a.nice-link{:href => "#"} consectetur
    adipiscing elit fusce vel sapien elit in malesuada semper mi,
    id
    %a.nice-link{:href => "#"} sollicitudin urna fermentum ut fusce
    varius nisl ac ipsum gravida vel pretium tellus.